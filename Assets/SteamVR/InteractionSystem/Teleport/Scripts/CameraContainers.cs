using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraContainers : MonoBehaviour
{

    public bool busy;
    // Start is called before the first frame update
    void Start()
    {
        busy = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
